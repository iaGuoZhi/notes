# netstat

* show connections: netstat -anp
* show listening ports: netstat -lntp
* show routing table: netstat -rn
* show network interface statistics: netstat -i
* show multicast group memberships: netstat -g
